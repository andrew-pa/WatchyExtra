[package]
name = "watchy_server"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = { version = "0.5.0-rc.1", features = ["json", "uuid"] }
chrono = "0.4"
serde = { version = "1", features = ["derive"] }
